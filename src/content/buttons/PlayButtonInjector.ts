export function initPlayButtonInjector(): void {
  // Phase 3 enhancement: scan for long text blocks and inject an inline "Play" button.
  // MVP: Popup entrypoint is sufficient; keep as a no-op placeholder.
}

